<h1 id='change-log'>Change Log</h1><h2 id='17-07-2024'>17-07-2024</h2>
<ol>
<li>Added Enum Value GST_ON_Funding FEE in TransactionType</li>
<li>Exchange Info <br/>

<ul>
<li>Added optional query param <code>market</code></li>
<li>Added <code>markets</code>, <code>tags</code>, <code>assetPrecisions</code>, <code>conversionRates</code> in the response <br/></li>
<li>For each contract, from the response: <br/>
i. Removed: <code>marginAsset</code>, <code>maintMarginPercent</code> <br/>
ii. Added: <code>market</code>, <code>marginAssetsSupported</code>, <code>fundingFeeInterval</code> <br/></li>
</ul></li>
<li>Open Orders, Order History, Linked Orders <br/>
a. Added <code>baseAsset</code>, <code>quoteAsset</code> in response <br/>
b. Added <code>lockedMarginInMarginAsset</code>, <code>lockedMargin</code>, <code>stopPrice</code>, <code>marginAsset</code>, <code>leveragedQty</code> in Order History response <br/></li>
<li>Add Margin/ Reduce Margin <br/>
a. Added <code>asset</code> in response <br/></li>
<li>Get order using client order ID, Get order using ref ID <br/>
a. Added <code>lockedMarginInMarginAsset(optional)</code>, <code>baseAsset</code>, <code>quoteAsset</code>, <code>marginAsset</code> in response <br/></li>
<li>Place Order <br/>
a. Added optional field <code>marginAsset</code> in body <br/>
b. Added <code>lockedMarginInMarginAsset</code>, <code>baseAsset</code>, <code>quoteAsset</code>, <code>marginAsset</code> in response <br/></li>
<li>Transaction History, Get Transaction using ID <br/>
a. Added baseAsset, quoteAsset in response <br/></li>
<li>Trade History, Get Trade using ID <br/>
a. Added <code>baseAsset</code>, <code>quoteAsset</code> , <code>marginAsset</code>, <code>feeInMarginAsset(optional)</code>, <code>realizedProfitInMarginAsset</code>(optional) in
response</li>
<li>Get Positions, Get Position using position id <br/>
a. Added baseAsset, quoteAsset , marginAsset, marginInMarginAsset(optional), realizedProfitInMarginAsset(optional) in
response</li>
<li>Web Socket Updates <br/>
a. New Events Added:<br/>
  i. Market Info ( marketInfo)<br/>
  ii. Mark Price Stream for All market ( markPriceArr)<br/>
  iii. All Market Tickers Streams( tickerArr)<br/>
b. Added new fields in the Response of following events <br/>
  i. Order Events : <code>marginAsset</code>, <code>market</code> , <code>baseAsset</code>, <code>quoteAsset</code><br/>
  ii. Position Events:<code>market</code>, <code>marginAsset</code>, <code>marginConversionRate</code>, <code>marginSettlementRate</code>, <code>marginInMarginAsset</code>,
<code>realizedProfitInMarginAsset</code> <br/>
 iii. Trade Events: <code>feeInMarginAsset</code> <br/></li>
</ol>
<h2 id='9-07-2024'>9-07-2024</h2>
<ol>
<li>Pagination update of following APIs: <code>orderHistory</code>, <code>tradeHistory</code>, <code>transactionHistory</code>, <code>openOrders</code>, <code>positions</code>
a. Removed <code>timestamp</code> query param.
b. Added optional query params namely:
   i. <code>startTimestamp</code> (lower bound),
   ii. <code>endTimestamp</code> (upper bound),
   iii. <code>sortOrder</code>(asc or desc),
c. Response: <code>nextTimestamp</code> which is used in the next paginated API call.
d. Examples added in the API descriptions section in the doc.</li>
<li>New Endpoint
a. Margin History ( GET /v1/order/fetch-margin-history)</li>
</ol>
<h2 id='19-06-2024'>19-06-2024</h2>
<ol>
<li>Updated Pagination of following APIs: <code>orderHistory</code>, <code>tradeHistory</code>, <code>transactionHistory</code>, <code>openOrders</code>
a. Pagination changed from page basis to timestamp basis.
b. Introduced timestamp basis pagination in positions API.
c. Response gives a field <code>nextTimestamp</code>, which is used for next API call.</li>
</ol>
<h2 id='06-06-2024'>06-06-2024</h2>
<ol>
<li>New Endpoints added
a. Get Depth Update( GET /v1/market/depth/:contractPair)<br/>
b. Get Aggregate trade update ( GET /v1/market/aggTrade/:contractPair)
c. Get order details using ref ID ( GET /v1/order/ref-id)
d. Get user id and account ID using ref ID ( GET /v1/integration/user)
e. Get fiat order details using reference ID ( GET /v1/integration/fiat-order-details)
f. Get 24hr ticker update ( GET /v1/market/ticker24Hr/:contractPair)
g. Get trade history based on trade ID ( GET /v1/user-data/trade-history/{id})
h. Get transaction history based on transaction ID ( GET /v1/user-data/transaction-history/{id})</li>
<li>Updated Endpoints
a. Two new optional query params( tradeId and positionId) added in ( GET /v1/user-data/transaction-history).
b. Updates in body of ( POST /v1/integration/event-replay)</li>
<li>Added optional field refId in the responses of place order, get order details using client order id, order history, open orders, linked
orders endpoints.</li>
<li>Secure Web Socket Updates:
a. In each event, eventTime will be in nanoseconds and type will be string.</li>
</ol>
<h2 id='28-05-2024'>28-05-2024</h2>
<ol>
<li>New Endpoints <br/>
a. Get Order ( GET /v1/order?clientOrderId=abc)<br/>
b. Get Position ( GET /v1/positions?positionId=abc)<br/>
c. Event Replay ( POST /v1/integration/event-replay): This is just endpoint, we&#39;re still deploying the changes.<br/>
<br/></li>
<li>Secure Web Socket Updates:<br/>
a. Added new event for Funding Fee named <strong>fundingFee</strong></li>
</ol>
<h2 id='20-05-2024'>20-05-2024</h2>
<ol>
<li>Added <code>lockedMargin</code> field in the place-order api response</li>
</ol>
<h2 id='10-05-2024'>10-05-2024</h2>
<ol>
<li>New Endpoint:
INR Deposit and Withdraw History ( GET /v1/integration/deposit-withdraw-history)</li>
<li>New enums: <code>PaymentType</code>, <code>PaymentStatus</code></li>
<li>Added optional query params for pagination for the following endpoints
a. Order History
b. Open Orders
c. Transaction History
d. Trade History</li>
<li>Updated public key sharing under Authenticated endpoints.</li>
<li>Secure Web Socket Updates:
a. Added eventTime field in all the events.
b. Updated response of order events.
In case of orderPartiallyFilled event, use filledAmount instead of cumQty for total filled
quantity.
c. New Events:
i. Margin Call ( marginCallAlert)
ii. Liquidation ( liquidationAlert)
iii. Trade stream (newTrade)</li>
</ol>
